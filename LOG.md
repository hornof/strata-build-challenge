2026-08-06 · F5 tail — --live recording run (new key works). Judge v1 scored on the golden set: 12 test cases (2 prompt excluded) → 10 correct · 1 safe · 1 wrong-with-confidence. The wrong-with-confidence is G-380.16-environmental-report-scope (labeled unsure, judged material — confident on a genuine toss-up); no false negatives, no false positives. The one safe: the binds-another-party trap, escalated to unsure rather than dismissed (conservative). 24 real recordings (synthetic:false) committed to recordings/; eval/score-report.md committed. Plain `python3 eval/harness.py` (replay, no key, no SDK) reproduces the score identically and exits 1 on the wrong-with-confidence — the honest v1 baseline the Act-2 demo improves. Unit suite stays green (50).

2026-08-06 · F5 golden set + harness — 7 tests green (50 total). eval/golden-set.jsonl: hand-labeled cases grounded in the Part 50 proceeding (Tribal Engagement Plan = material; terminology/renumber/typo = non-material; the cross-reference correction = non-material silence; binds-another-party and adjacent-section traps = non-material; two genuinely-ambiguous cases = unsure) plus reserved prompt-split examples. eval/harness.py grades correct / safe / wrong-with-confidence, reports false-negatives and false-positives separately, excludes prompt-split cases from scoring, and exits non-zero on any wrong-with-confidence (subprocess test with a seeded miss). README documents the exact test command. LIVE RUN BLOCKED: the .env key returns 401 authentication_error (invalid) — well-formed sk-ant- key, parsed cleanly, model string claude-opus-5 accepted; the server rejects the key. Per the roadmap halt/fallback the --live recording run + real judge-v1 score report move to the morning; golden set + harness + tests ship replay-ready. Flagged NEEDS-LUKE in the mailbox. (anthropic installed only in a throwaway venv for the attempted run; the repo stays dependency-free.)

2026-08-06 · F4 fixture + judge — 11 tests green (43 total). fixtures/company.json: Cascadia Grid Energy, 13 obligations (incl. O-013 siting, O-012 pre-filing, O-004 code-of-conduct, O-002 §35.28 ratings) + 3 projects + documents + trap obligations nothing in the corpus touches. src/judge.py is the only model call: strict-JSON output parsed defensively (malformed → unsure, never a crash), wrapped in two safeguards — a code citation check (verbatim match after quote/dash/whitespace normalization; unverified quote → unsure) and a refuter second call (credible refutation on the material/non-material axis → unsure). Record/replay keyed by a sha256 of the full request: replay is deterministic and needs no API key (anthropic imported lazily only in --live); a replay miss raises RecordingNotFound naming the --live fix. Tests replay synthetic recordings they write themselves; real recordings come from F5's --live run. dispositions.json is derived/gitignored. Model pinned to claude-opus-5 (per the Claude API skill), swappable via replay.

2026-08-06 · F3 differ — 7 tests green (32 total). src/differ.py diffs the proposed→final chain per docket into add/remove/modify/unchanged records over regulatory text only (instructions ignored); sections pair by number so a dropped section is loud; similarity is a difflib ratio in [0,1] used as a UI hint, never a status gate (status follows text equality). Corrections applied as patches to their base version: RM22-7's typo correction changes instruction text only, zero regulatory change (the honesty invariant). RM22-10 final = zero changed sections, a valid result. changes.json is derived/gitignored. Test 3 (RM22-10 zero) written first per the roadmap.

2026-08-06 · F2 parser — 14 tests green (25 total). Splits sections, separates regulatory text from amendatory instructions. Fixed the GPO page-break bug in clean(): a page break (blank·[[Page N]]·blank) was splitting instruction 17.c of RM22-7 final and leaking its tail into § 380.14's regulatory text (stale section pointer) — now the marker and one adjacent blank each side are removed so the instruction rejoins whole under § 380.16. Added correction base-version resolution: correction_of URL match, else most-recent non-correction version on/before the correction date (RM20-16's correction → the proposed rule, since correction_of is null). Reused spike parser.py; the fix is confined to clean(); parse() is now pure (no file writes), CLI writes sections.json (gitignored, derived).

2026-08-06 · F1 fetcher + corpus — 11 tests green. kind_of covers proposed/final/correction incl. the title-suffix-only correction (RM20-16, correction_of null); append-only verified offline via injectable fetch_text; committed corpus is 8 documents / 2 corrections, every raw.txt non-empty, metadata complete so nothing downstream needs the network. Reused spike fetch.py (renamed src/fetcher.py, refactored write_document to be testable without the network) and the spike's already-fetched corpus.
