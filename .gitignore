.claude/
__pycache__/
*.pyc
.DS_Store
.env

# derived pipeline artifacts — regenerable from the raw corpus, never source of truth
sections.json
changes.json
dispositions.json

# rendered app screens + demo outputs — regenerable with `python3 src/app.py`
app/
